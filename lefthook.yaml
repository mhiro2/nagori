output:
  - summary
  - meta

pre-commit:
  parallel: true
  commands:
    fmt-check:
      run: make fmt-check
    lint:
      run: make lint
    typecheck:
      run: make desktop-typecheck

pre-push:
  parallel: true
  commands:
    deny-check:
      run: make deny-check
